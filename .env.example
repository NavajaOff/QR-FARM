# ============================================================================
# CONFIGURACION UNIFICADA PARA DESARROLLO LOCAL Y DOCKER
# ============================================================================
# Este archivo funciona tanto para ejecucion local como con Docker Compose
# Copia este archivo a .env y configura los valores segÃºn tu entorno
# 
# ADVERTENCIAS DE SEGURIDAD:
# - NUNCA commitees el archivo .env real al repositorio
# - CAMBIA TODAS las claves secretas y contraseÃ±as antes de usar en producciÃ³n
# - Las contraseÃ±as deben tener mÃ­nimo 12 caracteres y ser complejas
# - NO uses valores por defecto en producciÃ³n
# ============================================================================

# ============================================================================
# ConfiguraciÃ³n de Flask
# ============================================================================
FLASK_ENV=production
DEBUG=False

# OBLIGATORIO: Genera una clave secreta Ãºnica y segura (mÃ­nimo 64 caracteres)
# NO uses este valor en producciÃ³n. Genera uno nuevo con:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=GENERAR_CLAVE_SECRETA_UNICA_Y_SEGURA_AQUI_NO_USAR_EN_PRODUCCION

FLASK_APP=app.py

# ============================================================================
# ConfiguraciÃ³n de Base de Datos
# NOTA: En Docker, estas variables se sobrescriben por docker-compose.yml
# DB_HOST se establece automÃ¡ticamente como mysql dentro de Docker
# ============================================================================
DB_HOST=mysql
DB_PORT=3306
DB_USER=root

# SEGURIDAD: En producciÃ³n, configura una contraseÃ±a segura para MySQL
# El valor vacÃ­o es solo para desarrollo. En producciÃ³n DEBE tener contraseÃ±a.
DB_PASSWORD=

DB_NAME=gestion_ganadera

# ============================================================================
# ConfiguraciÃ³n CORS (OBLIGATORIO)
# En producciÃ³n, limita solo a los dominios que realmente necesitas
# ============================================================================
# Desarrollo local (Docker):
CORS_ORIGINS=http://localhost:5174,http://localhost:5173,http://localhost:80

# ProducciÃ³n: Reemplaza con tus dominios reales:
# CORS_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com

# ============================================================================
# URL del Backend para el Frontend (OBLIGATORIO)
# ============================================================================
# Desarrollo (Docker):
VITE_BACKEND_URL=http://localhost:5010

# ProducciÃ³n: Cambia a tu dominio HTTPS real:
# VITE_BACKEND_URL=https://api.tu-dominio.com

# ============================================================================
# Super Administrador (creado automÃ¡ticamente)
# CAMBIA estas credenciales antes de usar en producciÃ³n
# ============================================================================
ROOT_SUPER_ADMIN_EMAIL=superadmin@qrfarm.com

# OBLIGATORIO: Cambia esta contraseÃ±a por una segura (mÃ­nimo 12 caracteres)
# Debe contener: mayÃºsculas, minÃºsculas, nÃºmeros y caracteres especiales
ROOT_SUPER_ADMIN_PASSWORD=GENERAR_CONTRASEÃ‘A_SEGURA_AQUI_MINIMO_12_CARACTERES

ROOT_SUPER_ADMIN_NOMBRE=Super Administrador QR-Farm

# ============================================================================
# JWT Secret Key (para tokens de autenticaciÃ³n)
# OBLIGATORIO: Genera una clave JWT Ãºnica y segura (mÃ­nimo 64 caracteres)
# NO uses este valor en producciÃ³n. Genera uno nuevo con:
# python -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANTE: Debe ser DIFERENTE de SECRET_KEY
# ============================================================================
JWT_SECRET_KEY=GENERAR_CLAVE_JWT_UNICA_Y_SEGURA_AQUI_NO_USAR_EN_PRODUCCION

# ============================================================================
# ConfiguraciÃ³n del Backend (Opcional, para logs y URLs)
# ============================================================================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=5000

# ============================================================================
# INSTRUCCIONES PARA GENERAR CLAVES SEGURAS
# ============================================================================
# Ejecuta estos comandos para generar claves seguras:
#
# Generar SECRET_KEY (64 caracteres hexadecimales):
# python -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
#
# Generar JWT_SECRET_KEY (64 caracteres hexadecimales):
# python -c "import secrets; print('JWT_SECRET_KEY=' + secrets.token_hex(32))"
#
# IMPORTANTE: AsegÃºrate de que SECRET_KEY y JWT_SECRET_KEY sean DIFERENTES
#
# ============================================================================
# CONFIGURACIÃ“N PARA DESARROLLO LOCAL (fuera de Docker)
# ============================================================================
# Para desarrollo local, modifica estas variables:
# - DB_HOST=localhost
# - FLASK_ENV=development
# - VITE_BACKEND_URL=http://localhost:5000
# - CORS_ORIGINS puede estar vacÃ­o (usa valores por defecto seguros)
# - DEBUG=True (solo en desarrollo)
#
# ============================================================================
# CHECKLIST DE SEGURIDAD ANTES DE PRODUCCIÃ“N
# ============================================================================
# [ ] Cambiar SECRET_KEY por una clave Ãºnica generada
# [ ] Cambiar JWT_SECRET_KEY por una clave Ãºnica generada (diferente de SECRET_KEY)
# [ ] Cambiar ROOT_SUPER_ADMIN_PASSWORD por una contraseÃ±a segura y compleja
# [ ] Configurar DB_PASSWORD con una contraseÃ±a segura para MySQL
# [ ] Actualizar CORS_ORIGINS solo con dominios de producciÃ³n (HTTPS)
# [ ] Actualizar VITE_BACKEND_URL con el dominio real de producciÃ³n (HTTPS)
# [ ] Verificar que DEBUG=False
# [ ] Verificar que FLASK_ENV=production
# [ ] Verificar que el archivo .env SÃ estÃ© en .gitignore (para NO committearlo)
# [ ] Configurar firewall y restricciones de acceso adecuadas
# ============================================================================